management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"

  #RabbitMQ connection details
spring:
  rabbitmq:
    host: localhost
    port: 5672
  #     username: guest
  #     password: guest
  zipkin:
    base-url: "http://localhost:9411/"
    enabled: true
  cloud:
    vault:
      kv:
        enabled: true
        backend: secret
      port: 8200
      host: localhost
      scheme: http
      authentication: token
      token: s.MLT0aZ4FLacSBKQl6bfuw86


logging:
  level:
    org.springframework: DEBUG

